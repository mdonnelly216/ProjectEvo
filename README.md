# ProjectEvo
Project for 410

--Database
    export the dump into your own mysql client, might have to change the connection username and password on the php files
    there is currently one username and password for admin, which is admin/admin123
    make sure php is downloaded (php -v in terminal) go to php ini file and unsemicolon extension=pdo_mysql and extension=mysqli then start php server in terminal with php -S localhost:8000
